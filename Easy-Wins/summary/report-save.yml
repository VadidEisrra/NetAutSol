#
# Saves all Ansible host variables into YAML file
#
---
- file: path={{ results }} state=directory
  run_once: True
- copy:
    content: "{{hostvars[inventory_hostname] | to_nice_yaml(indent=4)}}"
    dest: "{{results}}/{{inventory_hostname}}.yml"
