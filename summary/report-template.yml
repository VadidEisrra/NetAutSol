#
# Variables
#   results - output directory
#   fmt     - desired template
#   dest    - destination file name
#
---
- template: 
    src: "{{fmt|default('report-text.j2')}}"
    dest: "{{results}}/{{output|default('summary.log')}}"
  vars:
    allhosts: "{{hostvars}}"
  run_once: true
